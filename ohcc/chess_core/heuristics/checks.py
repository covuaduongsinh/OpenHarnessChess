"""Check / mate pattern helpers (stub)."""

from __future__ import annotations


def is_in_check_hint(fen: str) -> bool | None:
    """Return None until move-generation exists; Arasan can answer definitively."""
    _ = fen
    return None
